{
  "name": "web-bluetooth-parsers",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "pika-pack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@pika/pack": "^0.5.0",
    "@pika/plugin-build-web": "^0.9.2",
    "@pika/plugin-ts-standard-pkg": "^0.9.2",
    "typescript": "^4.0.3"
  },
  "@pika/pack": {
    "pipeline": [
      [
        "@pika/plugin-ts-standard-pkg",
        {}
      ],
      [
        "@pika/plugin-build-web"
      ]
    ]
  }
}
